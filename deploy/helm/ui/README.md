# musematic-ui

![Version: 0.1.0](https://img.shields.io/badge/Version-0.1.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: 0.1.0](https://img.shields.io/badge/AppVersion-0.1.0-informational?style=flat-square)

Next.js frontend for Musematic.

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| env | object | `{"NEXT_PUBLIC_API_URL":"http://localhost:8000","NEXT_PUBLIC_APP_ENV":"production","NEXT_PUBLIC_WS_URL":"ws://localhost:8000/ws"}` | Configures `env` for the ui chart. |
| env.NEXT_PUBLIC_API_URL | string | `"http://localhost:8000"` | Configures `env.NEXT_PUBLIC_API_URL` for the ui chart. |
| env.NEXT_PUBLIC_APP_ENV | string | `"production"` | Configures `env.NEXT_PUBLIC_APP_ENV` for the ui chart. |
| env.NEXT_PUBLIC_WS_URL | string | `"ws://localhost:8000/ws"` | Configures `env.NEXT_PUBLIC_WS_URL` for the ui chart. |
| image | object | `{"pullPolicy":"IfNotPresent","repository":"ghcr.io/musematic/ui","tag":"latest"}` | Configures `image` for the ui chart. |
| image.pullPolicy | string | `"IfNotPresent"` | Configures `image.pullPolicy` for the ui chart. |
| image.repository | string | `"ghcr.io/musematic/ui"` | Configures `image.repository` for the ui chart. |
| image.tag | string | `"latest"` | Configures `image.tag` for the ui chart. |
| replicaCount | int | `1` | Configures `replicaCount` for the ui chart. |
| resources | object | `{"limits":{"cpu":"500m","memory":"512Mi"},"requests":{"cpu":"100m","memory":"128Mi"}}` | Configures `resources` for the ui chart. |
| resources.limits | object | `{"cpu":"500m","memory":"512Mi"}` | Configures `resources.limits` for the ui chart. |
| resources.limits.cpu | string | `"500m"` | Configures `resources.limits.cpu` for the ui chart. |
| resources.limits.memory | string | `"512Mi"` | Configures `resources.limits.memory` for the ui chart. |
| resources.requests | object | `{"cpu":"100m","memory":"128Mi"}` | Configures `resources.requests` for the ui chart. |
| resources.requests.cpu | string | `"100m"` | Configures `resources.requests.cpu` for the ui chart. |
| resources.requests.memory | string | `"128Mi"` | Configures `resources.requests.memory` for the ui chart. |
| service | object | `{"nodePort":null,"port":3000,"type":"ClusterIP"}` | Configures `service` for the ui chart. |
| service.nodePort | string | `nil` | Configures `service.nodePort` for the ui chart. |
| service.port | int | `3000` | Configures `service.port` for the ui chart. |
| service.type | string | `"ClusterIP"` | Configures `service.type` for the ui chart. |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.13.1](https://github.com/norwoodj/helm-docs/releases/v1.13.1)
