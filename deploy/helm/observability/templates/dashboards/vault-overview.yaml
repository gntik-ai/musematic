apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-vault-overview
  namespace: {{ include "musematic-observability.namespace" . }}
  labels:
{{ include "musematic-observability.dashboardLabels" . | indent 4 }}
data:
  vault-overview.json: |
{{ .Files.Get "dashboards/vault-overview.json" | indent 4 }}
