# musematic-minio

![Version: 0.1.0](https://img.shields.io/badge/Version-0.1.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: latest](https://img.shields.io/badge/AppVersion-latest-informational?style=flat-square)

MinIO S3-compatible object storage for Musematic platform.

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| bucketInit | object | `{"enabled":true}` | Configures `bucketInit` for the minio chart. |
| bucketInit.enabled | bool | `true` | Configures `bucketInit.enabled` for the minio chart. |
| buckets | object | `{"executionArtifactsRetentionDays":90}` | Configures `buckets` for the minio chart. |
| buckets.executionArtifactsRetentionDays | int | `90` | Configures `buckets.executionArtifactsRetentionDays` for the minio chart. |
| clusterName | string | `"musematic-minio"` | Configures `clusterName` for the minio chart. |
| consoleEnabled | bool | `true` | Configures `consoleEnabled` for the minio chart. |
| createNamespace | bool | `true` | Configures `createNamespace` for the minio chart. |
| images | object | `{"mc":{"repository":"minio/mc","tag":"latest"},"minio":{"repository":"minio/minio","tag":"latest"}}` | Configures `images` for the minio chart. |
| images.mc | object | `{"repository":"minio/mc","tag":"latest"}` | Configures `images.mc` for the minio chart. |
| images.mc.repository | string | `"minio/mc"` | Configures `images.mc.repository` for the minio chart. |
| images.mc.tag | string | `"latest"` | Configures `images.mc.tag` for the minio chart. |
| images.minio | object | `{"repository":"minio/minio","tag":"latest"}` | Configures `images.minio` for the minio chart. |
| images.minio.repository | string | `"minio/minio"` | Configures `images.minio.repository` for the minio chart. |
| images.minio.tag | string | `"latest"` | Configures `images.minio.tag` for the minio chart. |
| minio | object | `{"enabled":true}` | Configures `minio` for the minio chart. |
| minio.enabled | bool | `true` | Configures `minio.enabled` for the minio chart. |
| namespace | string | `"platform-data"` | Configures `namespace` for the minio chart. |
| networkPolicy | object | `{"enabled":true}` | Configures `networkPolicy` for the minio chart. |
| networkPolicy.enabled | bool | `true` | Configures `networkPolicy.enabled` for the minio chart. |
| resources | object | `{"limits":{"cpu":"1000m","memory":"2Gi"},"requests":{"cpu":"500m","memory":"1Gi"}}` | Configures `resources` for the minio chart. |
| resources.limits | object | `{"cpu":"1000m","memory":"2Gi"}` | Configures `resources.limits` for the minio chart. |
| resources.limits.cpu | string | `"1000m"` | Configures `resources.limits.cpu` for the minio chart. |
| resources.limits.memory | string | `"2Gi"` | Configures `resources.limits.memory` for the minio chart. |
| resources.requests | object | `{"cpu":"500m","memory":"1Gi"}` | Configures `resources.requests` for the minio chart. |
| resources.requests.cpu | string | `"500m"` | Configures `resources.requests.cpu` for the minio chart. |
| resources.requests.memory | string | `"1Gi"` | Configures `resources.requests.memory` for the minio chart. |
| s3 | object | `{"accessKey":"","bucketPrefix":"platform","endpointUrl":"","region":"us-east-1","secretKey":""}` | Configures `s3` for the minio chart. |
| s3.accessKey | string | `""` | Configures `s3.accessKey` for the minio chart. |
| s3.bucketPrefix | string | `"platform"` | Configures `s3.bucketPrefix` for the minio chart. |
| s3.endpointUrl | string | `""` | Configures `s3.endpointUrl` for the minio chart. |
| s3.region | string | `"us-east-1"` | Configures `s3.region` for the minio chart. |
| s3.secretKey | string | `""` | Configures `s3.secretKey` for the minio chart. |
| servers | int | `4` | Configures `servers` for the minio chart. |
| standalone | bool | `false` | Configures `standalone` for the minio chart. |
| storageClass | string | `"standard"` | Configures `storageClass` for the minio chart. |
| storageSize | string | `"100Gi"` | Configures `storageSize` for the minio chart. |
| volumesPerServer | int | `4` | Configures `volumesPerServer` for the minio chart. |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.13.1](https://github.com/norwoodj/helm-docs/releases/v1.13.1)
