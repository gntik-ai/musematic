{{- if and .Values.minio.enabled .Values.createNamespace }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "musematic.minio.namespace" . }}
  annotations:
    helm.sh/resource-policy: keep
{{- end }}
