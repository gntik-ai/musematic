# musematic-clamav

![Version: 0.1.0](https://img.shields.io/badge/Version-0.1.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: 1.4](https://img.shields.io/badge/AppVersion-1.4-informational?style=flat-square)

In-cluster ClamAV daemon for DPA virus scanning (UPD-051 / R3).

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| freshclam | object | `{"database_mirror":"database.clamav.net","schedule":"0 3 * * *"}` | Daily signature refresh schedule (UTC). Runs as an initContainer at pod startup AND a sidecar via a CronJob; both write to the shared PVC. |
| freshclam.database_mirror | string | `"database.clamav.net"` | ClamAV signature mirror. |
| freshclam.schedule | string | `"0 3 * * *"` | Cron schedule for signature DB refresh. |
| image | object | `{"pullPolicy":"IfNotPresent","repository":"bitnami/clamav","tag":"1.4.4-debian-12-r0"}` | Container image for the ClamAV daemon. Bitnami's image bundles clamd + freshclam. |
| image.pullPolicy | string | `"IfNotPresent"` | Image pull policy. |
| image.repository | string | `"bitnami/clamav"` | Image registry/name. |
| image.tag | string | `"1.4.4-debian-12-r0"` | Image tag (clamd version). |
| namespace | string | `"platform-data"` | Configures `namespace` for the clamav chart. |
| networkPolicy | object | `{"egressMirrorHosts":["database.clamav.net"],"enabled":true}` | Egress-only NetworkPolicy. Allows DNS + HTTPS to the upstream signature mirror; denies all other traffic. |
| resources | object | `{"limits":{"cpu":1,"memory":"2Gi"},"requests":{"cpu":"100m","memory":"1Gi"}}` | Pod resource requests + limits. clamd RSS scales with the loaded signature database; 1 GiB is the documented default for v1.4+. |
| securityContext | object | `{"fsGroup":1001,"runAsGroup":1001,"runAsNonRoot":true,"runAsUser":1001,"seccompProfile":{"type":"RuntimeDefault"}}` | Pod security context. clamd runs as the bitnami clamav user (uid 1001) under a read-only-rootfs except for /var/lib/clamav. |
| service | object | `{"port":3310,"type":"ClusterIP"}` | Service exposing the clamd TCP socket on port 3310 cluster-internally. |
| service.port | int | `3310` | TCP port. |
| service.type | string | `"ClusterIP"` | Service type. ClusterIP only — never exposed externally. |
| storage | object | `{"className":"standard","size":"2Gi"}` | PersistentVolumeClaim for the signature database. The freshclam sidecar refreshes signatures daily; 2 GiB is generous for the main + daily + bytecode signature files. |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.13.1](https://github.com/norwoodj/helm-docs/releases/v1.13.1)
