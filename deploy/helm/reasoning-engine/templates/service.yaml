apiVersion: v1
kind: Service
metadata:
  name: reasoning-engine
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: reasoning-engine
  ports:
    - name: grpc
      port: {{ .Values.grpcPort }}
      targetPort: grpc
