# musematic-public-pages

![Version: 0.1.0](https://img.shields.io/badge/Version-0.1.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: 0.1.0](https://img.shields.io/badge/AppVersion-0.1.0-informational?style=flat-square)

Operationally-independent public pages release (UPD-051 sub-processors page, UPD-049 status page) — rule 49 outage independence.

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| image | object | `{"pullPolicy":"IfNotPresent","repository":"ghcr.io/gntik-ai/musematic-web","tag":"main"}` | Container image. Same Next.js bundle as the main `apps/web` release; uses a route-group filter to serve only `(public)/*`. |
| image.pullPolicy | string | `"IfNotPresent"` | Image pull policy. |
| image.repository | string | `"ghcr.io/gntik-ai/musematic-web"` | Image registry/name. |
| image.tag | string | `"main"` | Image tag (matches the main web release). |
| ingress | object | `{"annotations":{},"className":"nginx","enabled":true,"host":"musematic.ai","paths":[{"path":"/legal/sub-processors","pathType":"Exact"},{"path":"/legal/sub-processors.rss","pathType":"Exact"}],"tls":{"enabled":true,"secretName":"musematic-public-pages-tls"}}` | Public Ingress for `/legal/*` paths. Routes only the operationally- independent surfaces; the main app's Ingress handles everything else. |
| namespace | string | `"platform-public"` | Configures `namespace` for the public-pages chart. |
| postgres | object | `{"replicaDsnSecretRef":{"key":"dsn","name":"public-pages-postgres-replica-dsn"}}` | Read-only replica DSN env var. Operators set this to a replica pointing at the public-readable subset (sub_processors only). |
| replicas | int | `2` | Replica count. Two for HA — this Deployment must NOT depend on the main control-plane being healthy (rule 49). |
| resources.limits.cpu | string | `"500m"` |  |
| resources.limits.memory | string | `"512Mi"` |  |
| resources.requests.cpu | string | `"100m"` |  |
| resources.requests.memory | string | `"256Mi"` |  |
| securityContext.runAsGroup | int | `1001` |  |
| securityContext.runAsNonRoot | bool | `true` |  |
| securityContext.runAsUser | int | `1001` |  |
| securityContext.seccompProfile.type | string | `"RuntimeDefault"` |  |
| service.port | int | `3000` |  |
| service.type | string | `"ClusterIP"` |  |
| snapshot | object | `{"configmapName":"public-pages-sub-processors-snapshot"}` | ConfigMap-backed snapshot fallback. The regenerator cron writes the latest sub-processors JSON here; the public Deployment reads from it when the PostgreSQL replica is unreachable. |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.13.1](https://github.com/norwoodj/helm-docs/releases/v1.13.1)
