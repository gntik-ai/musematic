# Multi-Agent Work

Use workflows or fleets when several agents need to collaborate. The platform can route tasks, record intermediate outputs, enforce policies, and preserve the execution graph.

Avoid manual copy-paste between agents for regulated work. Manual transfer loses policy evidence and makes later debugging harder. When a fleet is involved, review lead, member, and conflict-resolution rules.
