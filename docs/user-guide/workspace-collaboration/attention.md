# Attention Requests

Attention requests ask a user to review, approve, or unblock work. They appear in realtime through the `attention` WebSocket channel and in the workspace UI.

Open the request, inspect the linked goal or execution, and respond with the requested decision. If the request is stale or unclear, comment in the workspace and ask the workflow owner to reissue it.
