# Fleets

Fleets owns coordinated groups of agents, membership, lead assignment, observer assignment, policy bindings, governance, and fleet learning.

Primary entities include fleets, members, policy bindings, observers, orchestration rules, governance records, and adaptation logs. REST APIs manage fleet state and learning operations. Events flow to runtime and analytics.

Fleet governance errors use stable codes documented in the API error catalog.
