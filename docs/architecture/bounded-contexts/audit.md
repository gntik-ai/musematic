# Audit

Audit owns immutable evidence for security, compliance, and operational review.

Primary entities include audit events, audit-chain entries, attestations, and verification results. The REST surface supports search, chain verification, signed attestation export, and evidence lookup. Events from other contexts append into the chain.

Audit records must retain correlation IDs, GIDs where available, actor, workspace, action, and outcome.
