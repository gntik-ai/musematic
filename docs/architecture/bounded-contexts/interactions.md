# Interactions

Interactions owns conversations, messages, participants, branches, merge records, workspace-goal messages, and attention requests.

Primary entities include conversations, interaction messages, participants, branches, and attention requests. REST APIs manage conversations and attention. Events are emitted on `interaction.events` and `interaction.attention`.

Interactions is the user-facing path that connects conversation activity to workflow execution and GID correlation.
