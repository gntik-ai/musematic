# Discovery

Discovery owns marketplace search, experiment workflows, hypothesis ranking, tournament scoring, and recommendation surfaces.

Primary entities include search indexes, experiment records, hypotheses, tournament scores, and recommendation payloads. REST APIs expose discovery and experiment actions. Events feed analytics and audit.

Discovery reads from Registry, Qdrant, OpenSearch, and analytics rollups.
