# Reasoning

Reasoning owns branch orchestration, budget enforcement, chain-of-thought trace persistence, and adaptive reasoning mode selection through the Go Reasoning Engine.

Primary entities include reasoning requests, branch budgets, convergence records, and trace artifacts. The service exposes gRPC methods and streams trace data. Events are emitted on reasoning-related runtime topics.

Reasoning integrates with model catalog fallback, cost governance, and execution evidence.
