# Changelog

## Unreleased

- Added UPD-051 Data Lifecycle (Tenant and Workspace): workspace + tenant data export ZIPs (presigned URL, 7d/30d TTL), two-phase deletion with grace + anti-enumeration cancel-link, tenant deletion with 2PA + subscription preflight + final-export linkage + cascade across PostgreSQL/Qdrant/Neo4j/ClickHouse/OpenSearch/S3 (extending privacy_compliance.CascadeOrchestrator), DPA upload with ClamAV virus scan + Vault storage + SHA-256 hash + version bump, public sub-processors page with RSS + email subscribe, GDPR Article 28 evidence package composer, backup separation via key-destruction. Adds `data_lifecycle` BC, `data_lifecycle.events` Kafka topic, migration `111_data_lifecycle` (3 tables + WorkspaceStatus.pending_deletion enum), Grafana dashboard, 5 operator runbooks. 78 new unit tests covering export idempotency, deletion R10 anti-enumeration, tenant 2PA + subscription guards, DPA virus refusal, Article 28 manifest integrity.
- Added UPD-048 Public Signup at Default Tenant Only: default-tenant public signup with workspace/subscription auto-provisioning, opaque 404s on Enterprise signup surfaces, first-admin `/setup` with mandatory MFA, cross-tenant identity handling, onboarding, memberships, and tenant switcher support.
- Added UPD-047 Plans, Subscriptions, and Quotas: billing plan versioning, subscription lifecycle management, quota enforcement, overage authorization, metering, admin subscription operations, and billing observability/runbook coverage.
- Clarified the repository test taxonomy after the historical `apps/control-plane/tests/e2e` rename: control-plane docker-compose and in-process suites live under `apps/control-plane/tests/integration`, while the kind-based harness for feature 071 lives at repository-root `tests/e2e`.
