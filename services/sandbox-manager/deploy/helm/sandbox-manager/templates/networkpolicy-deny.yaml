apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "sandbox-manager.fullname" . }}-sandbox-deny
spec:
  podSelector:
    matchLabels:
      musematic/sandbox: "true"
  policyTypes:
    - Ingress
    - Egress
