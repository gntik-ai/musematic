apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Release.Name }}-sandbox-deny
spec:
  podSelector:
    matchLabels:
      musematic/sandbox: "true"
  policyTypes:
    - Ingress
    - Egress
