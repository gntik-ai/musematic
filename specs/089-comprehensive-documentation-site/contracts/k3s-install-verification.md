# k3s Install Verification

FR-607 verification requires a fresh Ubuntu 22.04+ VM.

| Check | Target | Result |
| --- | --- | --- |
| k3s install | Within guide target | Not run |
| Platform Helm install | Within 30 minutes total | Not run |
| Login and workflow smoke test | After install | Not run |

Record VM size, storage class, ingress mode, TLS mode, and command transcript.
