export default function MarketplacePage() {
  return (
    <section className="space-y-4">
      <div>
        <p className="text-sm font-semibold uppercase tracking-[0.2em] text-brand-accent">
          Marketplace
        </p>
        <h1 className="mt-2 text-3xl font-semibold">Agent marketplace</h1>
      </div>
      <p className="max-w-2xl text-muted-foreground">
        Marketplace discovery lands here. The route is available now so the
        home dashboard quick action has a stable destination.
      </p>
    </section>
  );
}
