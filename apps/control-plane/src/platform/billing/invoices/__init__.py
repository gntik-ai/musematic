"""UPD-052 invoices sub-bounded-context.

Local mirror of Stripe invoices keyed by ``stripe_invoice_id``. Powers the
workspace and tenant invoice list pages and feeds the cost-governance reports.
"""
